name: Build and Release V1.0.0

on:
  push:
    branches: [ main, master ]
    tags: [ 'v*' ]
  workflow_dispatch:

jobs:
  build-desktop:
    name: Build Desktop (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        include:
          - os: windows-latest
            suffix: "win"
          - os: macos-latest
            suffix: "mac-arm"
          - os: ubuntu-latest
            suffix: "linux"

    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install yt-dlp yt-dlp-ejs pyinstaller pillow

      - name: Build Binary
        run: python desktop/build.py
        shell: bash

      - name: Robust Artifact Collection
        run: |
          mkdir -p staging
          # Find the built binary/dmg in dist, ignoring hidden files and the directory itself
          # This is name-agnostic and handles spaces perfectly
          find desktop/dist -maxdepth 1 -mindepth 1 ! -name ".*" -exec cp -rv "{}" staging/ \;
        shell: bash

      - name: Upload Artifact
        uses: actions/upload-artifact@v4
        with:
          name: youtube-downloader-${{ matrix.suffix }}
          path: staging/

  release:
    name: Create GitHub Release
    needs: build-desktop
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4

      - name: Download all artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Prepare Release Files
        run: |
          mkdir -p release_files
          # Specifically pick ONLY the final installers/binaries
          # This prevents uploading thousands of internal library files
          find artifacts -name "*.exe" -exec cp -v {} release_files/ \;
          find artifacts -name "*.dmg" -exec cp -v {} release_files/ \;
          # For Linux, we find the binary that was versioned with "_Linux"
          find artifacts -name "*_Linux" -exec cp -v {} release_files/ \;

      - name: Create Release and Upload
        uses: softprops/action-gh-release@v2
        with:
          files: release_files/*
          generate_release_notes: true
          draft: false
          prerelease: false
